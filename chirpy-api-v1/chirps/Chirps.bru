meta {
  name: Chirps
  type: http
  seq: 2
}

get {
  url: http://localhost:8080/api/chirps
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  # Gets all Chirps.
  QueryParams:
  
    `author_id={{user_id}}`
    `sort={{asc/desc}}`
}
